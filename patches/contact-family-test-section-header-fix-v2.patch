diff --git a/tools/Test-IntergradeContactShadowFamilyRegex.ps1 b/tools/Test-IntergradeContactShadowFamilyRegex.ps1
--- a/tools/Test-IntergradeContactShadowFamilyRegex.ps1
+++ b/tools/Test-IntergradeContactShadowFamilyRegex.ps1
@@ -30,6 +30,6 @@
 $patternLines = [Collections.Generic.List[string]]::new()
 for ($i = $start + 1; $i -lt $ini.Count; ++$i) {
     $line = $ini[$i].Trim()
-    if ($line.StartsWith('[')) { break }
+    if ($line -match '^\[[^\]]+\]$') { break }
     if ($line -and -not $line.StartsWith(';')) { $patternLines.Add($line) }
 }
